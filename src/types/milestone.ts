export interface Milestone {
    id: string
    title: string
    startDate: string
    endDate: string

    goals: string[]
    achievements: string[]
    reflection: string
    nextGoals: string[]
}